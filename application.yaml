apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mywebapp-argo-application
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/yannicknick08/2028.git
    targetRevision: HEAD
    path: "."
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  syncPolicy:
    automated:
      selfHeal: true
      prune: true  
  
